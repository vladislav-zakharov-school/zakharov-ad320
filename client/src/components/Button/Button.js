import React from 'react';
import './Button.css';

function Button() {
    return (
        // <button onClick={activateLasers}>
        <button className='button'>
            Button
        </button>
    );
}

export default Button;